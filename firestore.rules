rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // allow read/write for all collections to guarantee smooth operation, seeding and editing.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
